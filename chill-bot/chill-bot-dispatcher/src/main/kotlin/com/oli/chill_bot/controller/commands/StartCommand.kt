package com.oli.chill_bot.controller.commands

/*class StartCommand(
    private val identifier: String,
    private val description: String,
    private val updateController: UpdateController
): BotCommand(identifier, description) {
    override fun execute(telegramClient: TelegramClient, user: User, chat: Chat, arguments: Array<out String>) {
        updateController.setView(
            generateSendMessageWithOptionsAndHeader(
                UpdateController.WHAT_TO_DO,
                listOf(
                    UpdateController.PREPARE_FOR_INTERVIEW
                    //MORE_MORE_SLEEP,
                )
            )
        )
    }
}*/